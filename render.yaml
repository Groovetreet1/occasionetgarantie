services:
  - type: web
    name: occasion-garantie
    env: node
    region: frankfurt
    plan: free
    rootDir: "Occasion&garantie"
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_VERSION
        value: "18"
